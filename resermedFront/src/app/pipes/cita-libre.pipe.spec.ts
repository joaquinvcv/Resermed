import { CitaLibrePipe } from './cita-libre.pipe';

describe('CitaLibrePipe', () => {
  it('create an instance', () => {
    const pipe = new CitaLibrePipe();
    expect(pipe).toBeTruthy();
  });
});
