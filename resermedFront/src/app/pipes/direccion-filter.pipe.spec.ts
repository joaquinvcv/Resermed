import { DireccionFilterPipe } from './direccion-filter.pipe';

describe('DireccionFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DireccionFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
