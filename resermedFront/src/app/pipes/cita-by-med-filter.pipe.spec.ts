import { CitaByMedFilterPipe } from './cita-by-med-filter.pipe';

describe('CitaByMedFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CitaByMedFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
