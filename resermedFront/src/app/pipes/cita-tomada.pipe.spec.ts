import { CitaTomadaPipe } from './cita-tomada.pipe';

describe('CitaTomadaPipe', () => {
  it('create an instance', () => {
    const pipe = new CitaTomadaPipe();
    expect(pipe).toBeTruthy();
  });
});
