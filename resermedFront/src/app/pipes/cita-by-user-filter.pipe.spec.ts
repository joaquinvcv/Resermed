import { CitaByUserFilterPipe } from './cita-by-user-filter.pipe';

describe('CitaByUserFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CitaByUserFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
